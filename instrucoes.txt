tenho esse novo codigo e design para a tela de vendas. 

<!DOCTYPE html>
<html lang="pt-BR">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>IWR Atelier PDV — Venda por Código</title>

  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700;800;900&family=Playfair+Display:wght@700;800&family=JetBrains+Mono:wght@500;700;800&display=swap" rel="stylesheet">

  <style>
    :root {
      --bg: #090b10;
      --bg-2: #101117;
      --sidebar: #0d1016;
      --panel: #11141c;
      --panel-2: #151922;
      --gold-1: #f4da86;
      --gold-2: #d6aa3a;
      --gold-3: #a87514;
      --gold-dark: #3a2a0d;
      --text: #f8fafc;
      --muted: #98a2b3;
      --soft: rgba(255,255,255,.08);
      --line: rgba(255,255,255,.10);
      --gold-line: rgba(244,218,134,.34);
      --success: #20e0c2;
      --danger: #ef4444;
      --radius: 24px;
      --shadow: 0 26px 80px rgba(0,0,0,.45);
    }

    * { box-sizing: border-box; margin: 0; padding: 0; }

    body {
      min-height: 100vh;
      background:
        radial-gradient(circle at 84% 6%, rgba(214,170,58,.18), transparent 28%),
        radial-gradient(circle at 16% 86%, rgba(244,218,134,.08), transparent 26%),
        linear-gradient(135deg, #07090e, #0d1016 52%, #06070b);
      color: var(--text);
      font-family: Inter, system-ui, sans-serif;
    }

    .app {
      min-height: 100vh;
      display: grid;
      grid-template-columns: 280px 1fr;
    }

    .sidebar {
      position: sticky;
      top: 0;
      height: 100vh;
      padding: 22px 16px;
      background: linear-gradient(180deg, rgba(13,16,22,.98), rgba(9,11,16,.98));
      border-right: 1px solid var(--line);
      display: flex;
      flex-direction: column;
      gap: 20px;
    }

    .brand {
      min-height: 82px;
      border: 1px solid var(--line);
      border-radius: 22px;
      display: flex;
      align-items: center;
      gap: 14px;
      padding: 0 18px;
      background: rgba(255,255,255,.025);
      box-shadow: inset 0 1px 0 rgba(255,255,255,.04);
    }

    .brand-mark {
      width: 38px;
      height: 38px;
      border-radius: 10px;
      display: grid;
      place-items: center;
      background: linear-gradient(135deg, var(--gold-1), var(--gold-2));
      color: #15100a;
      font-weight: 900;
      font-family: Playfair Display, serif;
      box-shadow: 0 12px 30px rgba(214,170,58,.25);
    }

    .brand strong {
      display: block;
      font-family: Playfair Display, serif;
      font-size: 1.55rem;
      line-height: .9;
      letter-spacing: -.05em;
    }

    .brand span {
      display: block;
      margin-top: 4px;
      color: var(--muted);
      font-size: .68rem;
      font-weight: 900;
      letter-spacing: .14em;
      text-transform: uppercase;
    }

    .operator-card {
      display: flex;
      gap: 12px;
      align-items: center;
      padding: 16px;
      border-radius: 16px;
      border: 1px solid var(--line);
      background: rgba(255,255,255,.025);
    }

    .operator-card small { display: block; color: var(--muted); font-weight: 700; font-size: .74rem; }
    .operator-card strong { display: block; font-size: .86rem; }

    .nav-group { display: grid; gap: 8px; }
    .nav-title {
      height: 44px;
      display: flex;
      align-items: center;
      justify-content: space-between;
      border: 1px solid var(--line);
      border-radius: 12px;
      padding: 0 14px;
      background: rgba(255,255,255,.025);
      color: var(--text);
      font-size: .74rem;
      font-weight: 900;
      letter-spacing: .08em;
      text-transform: uppercase;
    }

    .nav-item {
      min-height: 54px;
      border-radius: 16px;
      padding: 0 14px;
      display: flex;
      align-items: center;
      gap: 12px;
      color: var(--muted);
      font-size: .78rem;
      font-weight: 900;
      letter-spacing: .04em;
    }

    .nav-item.active {
      color: #15100a;
      background: linear-gradient(135deg, var(--gold-1), var(--gold-2));
      box-shadow: 0 18px 40px rgba(214,170,58,.25);
    }

    .nav-icon {
      width: 34px;
      height: 34px;
      border-radius: 10px;
      display: grid;
      place-items: center;
      background: rgba(214,170,58,.14);
      border: 1px solid rgba(214,170,58,.18);
    }

    .spacer { flex: 1; }

    .user-card {
      display: flex;
      align-items: center;
      gap: 12px;
      padding: 14px;
      border-radius: 14px;
      border: 1px solid var(--line);
      background: rgba(255,255,255,.025);
    }

    .avatar {
      width: 42px;
      height: 42px;
      border-radius: 50%;
      display: grid;
      place-items: center;
      background: linear-gradient(135deg, var(--gold-1), var(--gold-2));
      color: #15100a;
      font-weight: 900;
    }

    .user-card strong { display: block; max-width: 118px; overflow: hidden; text-overflow: ellipsis; white-space: nowrap; font-size: .86rem; }
    .user-card small { display: block; color: var(--muted); font-size: .64rem; font-weight: 900; letter-spacing: .12em; }
    .logout { margin-left: auto; height: 34px; padding: 0 12px; border-radius: 10px; border: 1px solid var(--line); background: transparent; color: var(--muted); font-size: .64rem; font-weight: 900; }

    .content {
      padding: 28px 34px 44px;
      min-width: 0;
    }

    .topbar {
      display: flex;
      align-items: center;
      gap: 14px;
      margin-bottom: 24px;
      color: var(--muted);
      font-size: .72rem;
      font-weight: 900;
      letter-spacing: .14em;
      text-transform: uppercase;
    }

    .topbar strong { color: var(--text); font-size: .9rem; letter-spacing: normal; text-transform: none; }

    .alert {
      min-height: 42px;
      display: flex;
      align-items: center;
      padding: 0 16px;
      margin-bottom: 22px;
      border-radius: 10px;
      border: 1px solid rgba(244,218,134,.44);
      background: rgba(214,170,58,.08);
      color: var(--gold-1);
      font-size: .84rem;
      font-weight: 800;
    }

    .hero-grid {
      display: grid;
      grid-template-columns: minmax(0, 1fr) minmax(310px, 380px);
      gap: 22px;
      margin-bottom: 24px;
    }

    .hero-card,
    .total-card,
    .quick-actions,
    .panel,
    .footer-checkout {
      border: 1px solid var(--line);
      background: rgba(17,20,28,.78);
      border-radius: var(--radius);
      box-shadow: var(--shadow);
      backdrop-filter: blur(16px);
      position: relative;
      overflow: hidden;
    }

    .hero-card::before,
    .total-card::before,
    .panel::before,
    .footer-checkout::before {
      content: "";
      position: absolute;
      inset: 0 0 auto 0;
      height: 1px;
      background: linear-gradient(90deg, transparent, rgba(244,218,134,.48), transparent);
    }

    .hero-card { padding: 34px 34px 30px; }

    .badges { display: flex; gap: 10px; margin-bottom: 18px; flex-wrap: wrap; }
    .badge {
      display: inline-flex;
      align-items: center;
      height: 28px;
      border-radius: 999px;
      padding: 0 12px;
      border: 1px solid var(--line);
      background: rgba(255,255,255,.035);
      color: var(--gold-1);
      font-size: .66rem;
      font-weight: 900;
      letter-spacing: .12em;
      text-transform: uppercase;
    }

    .badge.green { color: var(--success); border-color: rgba(32,224,194,.2); background: rgba(32,224,194,.06); }

    h1 {
      font-family: Playfair Display, serif;
      font-size: clamp(3.2rem, 6vw, 5rem);
      line-height: .92;
      letter-spacing: -.065em;
      margin-bottom: 18px;
    }

    .hero-card p { max-width: 780px; color: #b8c0cc; font-size: 1.03rem; line-height: 1.7; }

    .total-card {
      padding: 30px 26px;
      border-color: rgba(244,218,134,.26);
      background: linear-gradient(135deg, rgba(58,42,13,.58), rgba(17,20,28,.82));
      display: grid;
      align-content: center;
      gap: 16px;
    }

    .total-top { display: flex; justify-content: space-between; gap: 16px; color: var(--gold-1); font-size: .68rem; font-weight: 900; letter-spacing: .14em; text-transform: uppercase; }
    .money-xl { font-family: JetBrains Mono, monospace; font-size: clamp(2.4rem, 5vw, 4.1rem); line-height: 1; letter-spacing: -.08em; color: var(--gold-1); }
    .progress { height: 7px; border-radius: 999px; background: #05070c; overflow: hidden; }
    .progress span { display: block; width: 0%; height: 100%; background: linear-gradient(90deg, var(--gold-1), var(--gold-2)); }

    .quick-actions {
      min-height: 76px;
      display: flex;
      align-items: center;
      gap: 12px;
      padding: 16px;
      margin-bottom: 28px;
      box-shadow: none;
    }

    .btn {
      min-height: 44px;
      border-radius: 14px;
      padding: 0 20px;
      border: 1px solid var(--line);
      background: rgba(255,255,255,.035);
      color: var(--text);
      font-size: .69rem;
      font-weight: 900;
      letter-spacing: .13em;
      text-transform: uppercase;
      cursor: pointer;
    }

    .btn.primary {
      background: linear-gradient(135deg, var(--gold-1), var(--gold-2));
      border-color: rgba(244,218,134,.56);
      color: #17110a;
      box-shadow: 0 14px 32px rgba(214,170,58,.22);
    }

    .btn:disabled { opacity: .45; cursor: not-allowed; }

    .checkout-grid {
      display: grid;
      grid-template-columns: minmax(0, 1fr) minmax(420px, .95fr);
      gap: 24px;
      align-items: start;
    }

    .left-stack { display: grid; gap: 22px; }

    .panel.gold {
      background: linear-gradient(135deg, #f4d77c 0%, #d6aa3a 48%, #b48218 100%);
      border-color: rgba(255,235,160,.44);
      color: #17110a;
      box-shadow: 0 28px 70px rgba(0,0,0,.46), inset 0 1px 0 rgba(255,255,255,.42);
    }

    .panel.gold::after {
      content: "";
      position: absolute;
      inset: 0;
      pointer-events: none;
      background: radial-gradient(circle at 12% 8%, rgba(255,255,255,.32), transparent 22%);
    }

    .panel-inner { position: relative; z-index: 1; padding: 26px; }

    .panel-header {
      display: flex;
      align-items: center;
      justify-content: space-between;
      gap: 16px;
      padding-bottom: 18px;
      margin-bottom: 18px;
      border-bottom: 1px solid rgba(54,34,4,.18);
    }

    .panel-title { display: flex; align-items: center; gap: 14px; }

    .icon-gold {
      width: 44px;
      height: 44px;
      border-radius: 14px;
      display: grid;
      place-items: center;
      background: rgba(255,239,180,.34);
      border: 1px solid rgba(58,42,13,.12);
      color: rgba(42,31,10,.72);
    }

    .panel h2 { font-family: Playfair Display, serif; font-size: 1.28rem; letter-spacing: -.03em; color: #211609; }
    .panel p { margin-top: 4px; color: rgba(27,22,12,.66); font-size: .84rem; font-weight: 600; line-height: 1.45; }

    .scanner-box {
      height: 130px;
      border-radius: 18px;
      border: 1px dashed rgba(244,218,134,.44);
      background: #070b13;
      display: grid;
      place-items: center;
      color: #98a2b3;
      position: relative;
      overflow: hidden;
      box-shadow: inset 0 1px 0 rgba(255,255,255,.06), 0 18px 44px rgba(0,0,0,.22);
    }

    .scanner-box::before {
      content: "";
      position: absolute;
      top: 0;
      left: 0;
      right: 0;
      height: 3px;
      background: var(--danger);
      box-shadow: 0 0 18px rgba(239,68,68,.9);
      animation: laser 2.2s ease-in-out infinite;
    }

    @keyframes laser {
      0%,100% { transform: translateY(0); }
      50% { transform: translateY(127px); }
    }

    .scanner-symbol { color: var(--gold-2); font-family: JetBrains Mono, monospace; font-size: 2.2rem; letter-spacing: .15em; margin-bottom: 10px; text-align: center; }
    .scanner-box strong { display: block; text-align: center; color: #98a2b3; font-size: .7rem; font-weight: 900; letter-spacing: .16em; text-transform: uppercase; }

    .payment-grid { display: grid; grid-template-columns: 1fr 1fr; gap: 16px; }
    .field { display: grid; gap: 8px; }
    .field label { color: rgba(30,24,9,.82); font-size: .66rem; font-weight: 900; letter-spacing: .13em; text-transform: uppercase; }
    .input-box {
      min-height: 58px;
      display: flex;
      align-items: center;
      padding: 0 16px;
      border-radius: 14px;
      background: #070b13;
      color: var(--text);
      border: 1px solid rgba(255,255,255,.08);
      font-weight: 800;
      box-shadow: inset 0 1px 0 rgba(255,255,255,.06), 0 10px 24px rgba(0,0,0,.18);
    }
    .mono { font-family: JetBrains Mono, monospace; letter-spacing: -.04em; }

    .change-panel {
      grid-column: 1 / -1;
      display: grid;
      grid-template-columns: 1fr auto;
      align-items: center;
      gap: 18px;
      min-height: 88px;
      padding: 18px 20px;
      border-radius: 18px;
      background: linear-gradient(135deg, #070b13, #111827);
      border: 1px solid rgba(255,255,255,.10);
      box-shadow: 0 16px 38px rgba(0,0,0,.24), inset 0 1px 0 rgba(255,255,255,.08);
    }
    .change-panel span { display: block; color: var(--gold-1); font-size: .68rem; font-weight: 900; letter-spacing: .14em; text-transform: uppercase; margin-bottom: 5px; }
    .change-panel small { color: #cbd5e1; font-weight: 700; }
    .change-value { color: var(--success); font-family: JetBrains Mono, monospace; font-size: clamp(1.85rem, 4vw, 3rem); font-weight: 900; letter-spacing: -.08em; text-shadow: 0 0 22px rgba(32,224,194,.25); }

    .summary-card { min-height: 520px; }
    .summary-stats {
      display: grid;
      grid-template-columns: repeat(3, 1fr);
      gap: 12px;
      padding: 14px;
      border-radius: 18px;
      background: #070b13;
      border: 1px solid rgba(255,255,255,.08);
      margin-bottom: 22px;
    }
    .stat {
      min-height: 78px;
      padding: 14px;
      border-radius: 14px;
      background: #11141c;
      border: 1px solid rgba(255,255,255,.08);
    }
    .stat span { color: #98a2b3; font-size: .64rem; font-weight: 900; letter-spacing: .17em; text-transform: uppercase; }
    .stat strong { display: block; margin-top: 10px; color: #f8fafc; font-family: JetBrains Mono, monospace; font-size: 1.08rem; letter-spacing: -.04em; }
    .stat.total strong { color: var(--gold-1); }

    .empty-cart {
      min-height: 118px;
      display: grid;
      place-items: center;
      text-align: center;
      padding: 20px;
      border-radius: 18px;
      border: 1px dashed rgba(244,218,134,.32);
      background: #070b13;
      color: #d5d9e1;
      font-weight: 800;
      line-height: 1.45;
    }

    .clear-btn {
      min-height: 36px;
      padding: 0 18px;
      border-radius: 999px;
      border: 1px solid rgba(58,42,13,.22);
      background: rgba(255,239,180,.22);
      color: #4c350d;
      font-size: .62rem;
      font-weight: 900;
      letter-spacing: .12em;
      text-transform: uppercase;
    }

    .footer-checkout {
      margin-top: 24px;
      min-height: 94px;
      display: flex;
      align-items: center;
      justify-content: space-between;
      gap: 18px;
      padding: 22px;
      background: linear-gradient(135deg, #f4d77c, #d6aa3a 58%, #b48218);
      border-color: rgba(255,235,160,.44);
      color: #17110a;
      box-shadow: 0 28px 70px rgba(0,0,0,.36), inset 0 1px 0 rgba(255,255,255,.38);
    }
    .footer-checkout span { display: block; color: rgba(30,24,9,.66); font-size: .68rem; font-weight: 900; letter-spacing: .16em; text-transform: uppercase; }
    .footer-checkout strong { display: block; margin-top: 6px; font-family: JetBrains Mono, monospace; font-size: 2rem; letter-spacing: -.08em; }
    .footer-checkout .btn.primary { background: rgba(7,11,19,.90); color: #f8fafc; border-color: rgba(7,11,19,.8); min-width: 190px; }

    @media (max-width: 1180px) {
      .app { grid-template-columns: 1fr; }
      .sidebar { position: relative; height: auto; flex-direction: row; overflow-x: auto; align-items: center; }
      .brand, .operator-card, .nav-group, .user-card { flex: 0 0 auto; }
      .spacer { display: none; }
      .hero-grid, .checkout-grid { grid-template-columns: 1fr; }
    }

    @media (max-width: 760px) {
      .content { padding: 18px 14px 120px; }
      .sidebar { padding: 14px; }
      .quick-actions, .footer-checkout { flex-direction: column; align-items: stretch; }
      .btn { width: 100%; }
      .payment-grid, .summary-stats { grid-template-columns: 1fr; }
      .change-panel { grid-template-columns: 1fr; }
      .hero-card { padding: 26px; }
      h1 { font-size: clamp(2.7rem, 17vw, 4rem); }
    }
  </style>
</head>
<body>
  <div class="app">
    <aside class="sidebar">
      <div class="brand">
        <div class="brand-mark">I</div>
        <div>
          <strong>IWR.</strong>
          <span>Atelier PDV</span>
        </div>
      </div>

      <div class="operator-card">
        <div class="nav-icon">♛</div>
        <div>
          <strong>Operação premium</strong>
          <small>Vendas e promissórias</small>
        </div>
      </div>

      <nav class="nav-group">
        <div class="nav-title">Vendas <span>⌃</span></div>
        <div class="nav-item active"><span class="nav-icon">▣</span><div>PDV<br>Nova venda</div></div>
        <div class="nav-item"><span class="nav-icon">↺</span><div>Consultas<br>Histórico</div></div>
      </nav>

      <nav class="nav-group">
        <div class="nav-title">Clientes <span>⌄</span></div>
        <div class="nav-title">Produtos <span>⌄</span></div>
        <div class="nav-title">Notas <span>⌄</span></div>
        <div class="nav-title">Admin <span>⌄</span></div>
      </nav>

      <div class="spacer"></div>

      <div class="user-card">
        <div class="avatar">A</div>
        <div>
          <strong>Administra...</strong>
          <small>Vendedor</small>
        </div>
        <button class="logout">Sair</button>
      </div>
    </aside>

    <main class="content">
      <div class="topbar"><span>PDV</span><strong>Nova venda</strong></div>
      <div class="alert">Troque a senha padrão antes de operar em produção.</div>

      <section class="hero-grid">
        <div class="hero-card">
          <div class="badges"><span class="badge">★ Operação PDV</span><span class="badge green">Atelier IWR</span></div>
          <h1>Venda por código</h1>
          <p>Digite ou leia a etiqueta do produto para montar a venda, finalizar no backend e baixar o estoque automaticamente.</p>
        </div>

        <div class="total-card">
          <div class="total-top"><span>Subtotal</span><span>Total da venda</span></div>
          <strong class="money-xl">R$ 0,00</strong>
          <div class="progress"><span></span></div>
        </div>
      </section>

      <section class="quick-actions">
        <button class="btn primary">Nova venda</button>
        <button class="btn" disabled>Imprimir última</button>
        <button class="btn">Buscar produto</button>
      </section>

      <section class="checkout-grid">
        <div class="left-stack">
          <article class="panel gold">
            <div class="panel-inner">
              <header class="panel-header">
                <div class="panel-title">
                  <div class="icon-gold">▥</div>
                  <div>
                    <h2>Leitor de código</h2>
                    <p>Simule a leitura de um código de barras de luxo.</p>
                  </div>
                </div>
              </header>
              <div class="scanner-box">
                <div>
                  <div class="scanner-symbol">||||</div>
                  <strong>Toque para ler código</strong>
                </div>
              </div>
            </div>
          </article>

          <article class="panel gold">
            <div class="panel-inner">
              <header class="panel-header">
                <div class="panel-title">
                  <div class="icon-gold">$</div>
                  <div>
                    <h2>Pagamento</h2>
                    <p>Revise subtotal, desconto e forma de pagamento.</p>
                  </div>
                </div>
              </header>

              <div class="payment-grid">
                <div class="field">
                  <label>Forma de pagamento</label>
                  <div class="input-box">Dinheiro</div>
                </div>
                <div class="field">
                  <label>Desconto R$</label>
                  <div class="input-box mono">R$ 0,00</div>
                </div>
                <div class="field">
                  <label>Valor recebido</label>
                  <div class="input-box mono">R$ 0,00</div>
                </div>
                <section class="change-panel">
                  <div><span>Troco</span><small>Resultado calculado automaticamente</small></div>
                  <strong class="change-value">R$ 0,00</strong>
                </section>
              </div>
            </div>
          </article>
        </div>

        <article class="panel gold summary-card">
          <div class="panel-inner">
            <header class="panel-header">
              <div class="panel-title">
                <div class="icon-gold">◇</div>
                <div>
                  <h2>Resumo da venda</h2>
                  <p>Conferência rápida do carrinho de compras.</p>
                </div>
              </div>
              <button class="clear-btn">Limpar carrinho</button>
            </header>

            <div class="summary-stats">
              <div class="stat"><span>Itens</span><strong>0</strong></div>
              <div class="stat"><span>Desconto</span><strong>R$ 0,00</strong></div>
              <div class="stat total"><span>Total geral</span><strong>R$ 0,00</strong></div>
            </div>

            <div class="empty-cart">Nenhum item no carrinho. Leia um código ou busque um produto para começar.</div>
          </div>
        </article>
      </section>

      <section class="footer-checkout">
        <div>
          <span>Subtotal: R$ 0,00 — Desconto: R$ 0,00</span>
          <strong>R$ 0,00</strong>
        </div>
        <button class="btn primary">Finalizar venda</button>
      </section>
    </main>
  </div>
</body>
</html>